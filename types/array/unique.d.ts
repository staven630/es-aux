/**
 * 基本类型数组去重
 *
 * @param {Array} arr - 需要去重的数组
 */
export declare function uniqueArr(arr: any[]): any[];
/**
 * 对象数组单个键值匹配去重
 *
 * @param {Array} arr - 需要去重的数组
 * @param {String} key - 单个键值
 */
export declare function uniqueKeyInArr(arr: any[], key: string): any;
/**
 * 对象数组完全匹配去重
 *
 * @param {Array} arr - 需要去重的数组
 */
export declare function uniqueObjInArr(arr: any): any;
